components:
  # Time and Date Operations
  # Command: date +"%Y-%m-%d"                                      # Current date YYYY-MM-DD
  # Command: date +"%H:%M:%S"                                      # Current time HH:MM:SS
  # Command: date +"%Y-%m-%d %H:%M:%S"                             # Current date and time
  # Command: date -d "next Monday" +"%Y-%m-%d"                     # Get next Monday's date
  # Command: date -d "last Friday" +"%Y-%m-%d"                     # Get last Friday's date
  # Command: date -d "+7 days" +"%Y-%m-%d"                         # Date 7 days from now
  # Command: date -d "-3 days" +"%Y-%m-%d"                         # Date 3 days ago
  # Command: date -d "Jan 1 + 100 days" +"%Y-%m-%d"                # 100 days after Jan 1
  # Command: date -d "@$TIMESTAMP" +"%Y-%m-%d %H:%M:%S"            # Convert Unix timestamp to date
  # Command: date +%s                                              # Current Unix timestamp
  # Command: date --date="2023-01-01" +%s                          # Get timestamp from date
  # Command: TZ="America/New_York" date                            # Date in specific timezone
  # Command: TZ="UTC" date                                         # UTC date and time
  # Command: TZ="Asia/Tokyo" date                                  # Tokyo date and time
  # Command: cal                                                   # Calendar for current month
  # Command: cal 12 2023                                           # Calendar for Dec 2023
  # Command: cal -3                                                # Calendar for prev, current, next month
  # Command: cal -y 2023                                           # Full year calendar
  # Command: date -d "2023-01-01" +%A                              # Day of week for date
  # Command: date -d "2023-01-01" +%j                              # Day of year for date
  # Command: date -d "2023-01-01" +%U                              # Week number for date
  # Command: date -d "2023-12-31" +%V                              # ISO week number for date
  # Command: expr $(date -d "2023-12-31" +%j) / 7                  # Rough week of year
  # Command: date -d @$(($(date +%s) + 3600)) +"%H:%M:%S"          # Time 1 hour from now
  # Command: python3 -c "import time; print(time.time())"          # High precision Unix timestamp