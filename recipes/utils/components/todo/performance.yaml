components:
  # Performance Monitoring
  # Command: iostat                                                # IO statistics
  # Command: iostat -d 2                                           # IO stats every 2 seconds
  # Command: iostat -xz 2                                          # Extended IO stats
  # Command: vmstat                                                # Virtual memory statistics
  # Command: vmstat 2 5                                            # VM stats every 2 seconds, 5 times
  # Command: vmstat -s                                             # Memory usage summary
  # Command: sar                                                   # System activity reporter
  # Command: sar -u 2 5                                            # CPU usage every 2 seconds, 5 times
  # Command: sar -r 2 5                                            # Memory usage
  # Command: sar -b 2 5                                            # IO and transfer rate
  # Command: sar -n DEV 2 5                                        # Network statistics
  # Command: mpstat                                                # Multi-processor statistics
  # Command: mpstat -P ALL 2 5                                     # All CPU core stats every 2 seconds
  # Command: pidstat                                               # Per-process statistics
  # Command: pidstat -p $PID 2 5                                   # Stats for specific process
  # Command: pidstat -r -p $PID 2 5                                # Memory stats for process
  # Command: pidstat -d -p $PID 2 5                                # IO stats for process
  # Command: dstat                                                 # Versatile tool for statistics
  # Command: dstat -cdngyms 2                                      # CPU, disk, net, paging, sys stats
  # Command: iotop -o                                              # IO monitoring (only processes doing IO)
  # Command: htop                                                  # Interactive process viewer
  # Command: atop                                                  # Advanced system monitor
  # Command: nmon                                                  # Performance monitor
  # Command: uptime                                                # Show load average
  # Command: ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head     # Top CPU consuming processes
  # Command: ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head     # Top memory consuming processes
  # Command: free -h                                               # Human-readable memory usage
  # Command: /proc/loadavg                                         # Load average info
  # Command: /proc/meminfo                                         # Memory usage info
  # Command: /proc/net/dev                                         # Network device stats