components:
  # System Forensics and Analysis
  # Command: ps aux --sort=-%mem | head -10                         # Top 10 processes by memory
  # Command: ps aux --sort=-%cpu | head -10                         # Top 10 processes by CPU
  # Command: ps axo pid,user,pcpu,command | sort -k3 -r | head      # Process sort by CPU descending
  # Command: netstat -nlpt                                          # TCP listening ports with process
  # Command: netstat -nlpu                                          # UDP listening ports with process
  # Command: netstat -antup                                         # All TCP/UDP connections with PID
  # Command: netstat -an | grep ESTABLISHED | wc -l                 # Count established connections
  # Command: netstat -na | grep :80 | wc -l                         # Count connections to port 80
  # Command: lsof -u username                                       # Files opened by user
  # Command: lsof -p PID                                            # Files opened by PID
  # Command: lsof -i TCP:22                                         # Processes using TCP port 22
  # Command: lsof -i :80                                            # Processes using port 80
  # Command: lsof -i                                                # All network connections
  # Command: lsof -i -P -n                                          # Network connections (numerical)
  # Command: lsof +D /var/log                                       # Files open in directory
  # Command: ss -st                                                 # All TCP sockets
  # Command: ss -ltn                                                # Listening TCP sockets
  # Command: ss -lun                                                # Listening UDP sockets
  # Command: ss -s                                                  # Socket statistics
  # Command: find /home -type f -name "*.bash_history" -exec cat {} \; # Collect bash history
  # Command: history | grep ssh                                     # Find SSH commands in history
  # Command: cat ~/.bash_history | grep password                    # Find password in history
  # Command: find /var/log -type f | xargs grep -l "Failed password" # Find auth failure logs
  # Command: find /var/log -type f | xargs grep -l "authentication failure" # Auth failures
  # Command: journalctl -u sshd | grep "Failed"                     # SSH failures from journal