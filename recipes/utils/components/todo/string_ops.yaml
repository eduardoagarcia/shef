components:
  # String Manipulation
  # Command: cut -d',' -f1 file.csv                                # Extract first field from CSV file
  # Command: cut -c1-5 file.txt                                    # Extract first 5 characters
  # Command: tr 'a-z' 'A-Z' < file.txt                             # Convert lowercase to uppercase
  # Command: tr -d '\\t' < file.txt                                # Delete tabs from file
  # Command: tr -s ' ' < file.txt                                  # Squeeze repeated spaces into one
  # Command: paste file1 file2                                     # Merge lines from files
  # Command: paste -d',' file1 file2                               # Merge lines using delimiter
  # Command: join file1 file2                                      # Join files on common field
  # Command: join -t',' -1 2 -2 3 file1 file2                      # Join using specific fields
  # Command: comm file1 file2                                      # Compare sorted files
  # Command: comm -12 file1 file2                                  # Show only lines in both files
  # Command: comm -23 file1 file2                                  # Show only lines unique to file1
  # Command: comm -13 file1 file2                                  # Show only lines unique to file2
  # Command: fold -w 80 file.txt                                   # Wrap lines to 80 characters
  # Command: column -t file.txt                                    # Format file as table
  # Command: column -t -s',' file.csv                              # Format CSV as table
  # Command: shuf file.txt                                         # Randomize lines in file
  # Command: rev file.txt                                          # Reverse characters on each line
  # Command: expand file.txt                                       # Convert tabs to spaces
  # Command: unexpand file.txt                                     # Convert spaces to tabs
  # Command: echo ${string:0:5}                                    # Extract substring (first 5 chars)
  # Command: echo ${string//pattern/replacement}                   # Global search and replace
  # Command: echo ${string#prefix}                                 # Remove prefix
  # Command: echo ${string%suffix}                                 # Remove suffix
  # Command: echo ${string,,}                                      # Convert to lowercase
  # Command: echo ${string^^}                                      # Convert to uppercase