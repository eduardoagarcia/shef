components:
  # Mathematical Operations
  # Command: expr 10 + 5                                           # Basic addition
  # Command: expr 10 - 5                                           # Basic subtraction
  # Command: expr 10 \* 5                                          # Basic multiplication (escape *)
  # Command: expr 10 / 5                                           # Basic division (integer)
  # Command: expr 10 % 3                                           # Modulo
  # Command: echo "scale=2; 10/3" | bc                             # Floating point division
  # Command: echo "sqrt(16)" | bc -l                               # Square root
  # Command: echo "10^3" | bc                                      # Exponentiation
  # Command: echo "s($PI/6)" | bc -l                               # Sine
  # Command: echo "c($PI/3)" | bc -l                               # Cosine
  # Command: echo "a(1)*4" | bc -l                                 # Arctangent (pi calculation)
  # Command: echo "l(10)" | bc -l                                  # Natural logarithm
  # Command: echo "e(2)" | bc -l                                   # Exponential function
  # Command: echo "define abs(x) { if (x < 0) return -x; return x; } abs(-10)" | bc -l # Absolute value
  # Command: echo "scale=0; 10/3" | bc                             # Integer division with bc
  # Command: echo "obase=2; 10" | bc                               # Decimal to binary
  # Command: echo "obase=16; 255" | bc                             # Decimal to hex
  # Command: echo "ibase=2; 1010" | bc                             # Binary to decimal
  # Command: echo "ibase=16; FF" | bc                              # Hex to decimal
  # Command: seq 1 10 | paste -sd+ | bc                            # Sum series of numbers
  # Command: seq 1 10 | awk '{sum+=$1} END {print sum}'            # Alternative sum
  # Command: seq 1 10 | awk '{prod*=$1} END {print prod}'          # Product of series
  # Command: seq 1 10 | awk '{sum+=$1} END {print sum/NR}'         # Average of series
  # Command: seq 1 10 | sort -n | awk 'NR==int((NR+1)/2)'          # Median of series (odd)
  # Command: echo "scale=10; 4*a(1)" | bc -l                       # Pi calculation
  # Command: echo "scale=10; e(1)" | bc -l                         # e calculation
  # Command: jot -r 1 1 100                                        # Random number 1-100