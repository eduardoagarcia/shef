components:
  # Media File Operations
  # Command: file --mime-type -b filename                          # Get MIME type of media file
  # Command: identify -format "%wx%h" image.jpg                    # Get image dimensions
  # Command: convert image.jpg -resize 800x600 resized.jpg         # Resize image
  # Command: convert image.jpg -rotate 90 rotated.jpg              # Rotate image
  # Command: convert image.jpg -quality 80 compressed.jpg          # Compress image
  # Command: convert image.jpg -crop 300x300+100+100 cropped.jpg   # Crop image
  # Command: convert image.jpg -grayscale Rec709Luminance gray.jpg # Convert to grayscale
  # Command: convert image.jpg -flip flipped.jpg                   # Flip image vertically
  # Command: convert image.jpg -flop flopped.jpg                   # Flip image horizontally
  # Command: convert *.jpg -delay 100 animation.gif                # Create GIF from images
  # Command: convert image.jpg -format png image.png               # Convert between formats
  # Command: ffmpeg -i video.mp4 -ss 00:00:10 -frames:v 1 thumb.jpg # Extract video thumbnail
  # Command: ffmpeg -i video.mp4 -vn audio.mp3                     # Extract audio from video
  # Command: ffmpeg -i video.mp4 -t 60 clip.mp4                    # Cut first 60 seconds
  # Command: ffmpeg -i video.mp4 -vf "scale=800:600" resized.mp4   # Resize video
  # Command: ffmpeg -i video.mp4 -vcodec libx264 -crf 28 smaller.mp4 # Compress video
  # Command: ffmpeg -i audio.mp3 -af "volume=2" louder.mp3         # Adjust audio volume
  # Command: exiftool -S -ImageSize -DateTimeOriginal photo.jpg    # Extract basic EXIF data
  # Command: exiftool -all= photo.jpg                              # Remove all EXIF data
  # Command: mediainfo video.mp4                                   # Get media file information
  # Command: soxi audio.wav                                        # Audio file information
  # Command: sox audio.wav -r 44100 -b 16 converted.wav            # Convert audio format