components:
  # System Information and Monitoring
  # Command: uname -a                                              # Full system information
  # Command: hostname                                              # Get hostname
  # Command: whoami                                                # Current user
  # Command: id                                                    # User and group info
  # Command: uptime                                                # System uptime
  # Command: date                                                  # Current date and time
  # Command: df -h                                                 # Disk usage (human readable)
  # Command: du -sh /path                                          # Directory size (summarized, human readable)
  # Command: free -h                                               # Memory usage (human readable)
  # Command: top -b -n 1                                           # Snapshot of processes
  # Command: ps aux                                                # Process listing
  # Command: ps -p $PID -o %cpu,%mem,cmd                          # CPU and memory usage of process
  # Command: lsof -i :PORT                                         # List processes using specific port
  # Command: netstat -tuln                                         # List all listening ports
  # Command: lscpu                                                 # CPU information
  # Command: lsmem                                                 # Memory information
  # Command: lsblk                                                 # Block device information
  # Command: lspci                                                 # List PCI devices
  # Command: lsusb                                                 # List USB devices
  # Command: dmesg | tail                                          # Recent kernel messages
  # Command: sar -u 1 3                                            # CPU utilization (3 samples, 1 second apart)
  # Command: vmstat 1 3                                            # Virtual memory statistics
  # Command: iostat 1 3                                            # IO statistics
  # Command: w                                                     # Currently logged in users
  # Command: who                                                   # Users logged in
  # Command: last | head                                           # Recent logins
  # Command: printenv                                              # Print environment variables
  # Command: env                                                   # Print environment